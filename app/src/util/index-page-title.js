/**
 * 设置页面标题
 */

const titleConfig = {
  home: '港美股开户',
  login: '港美股账户登录',
  register: '港美股开户(1/4)',
  setTitle({
    name,
  }) {
    if (name) {
      // const title = titleConfig[name] || '';
      // if (title) {
      // } else {
      // }
    }
  },
};


module.exports = titleConfig;
